{
  "name": "barebones-invoice",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "packageManager": "pnpm@8.15.4",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "check:no-binaries": "bash ./scripts/check-no-binaries.sh --staged",
    "pre-commit": "pnpm run check:no-binaries"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}
